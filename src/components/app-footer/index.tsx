/** app 的底部 */
import React, { useState } from 'react';

interface IApp {}

const App: React.FC<IApp> = () => {
  const [data, setData] = useState(0);

  return <div>12</div>;
};

export default App;
